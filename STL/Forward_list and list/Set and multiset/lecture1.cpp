//count-->function tells if it is present or not
//set can't have duplicate values
//lower_bound returns the iterator to the element if it is present
//upper_bound returns the iterator to the element just greater if it is present
//upper_bound returns the iterator to the element just after the given element if it is present