//find elements of maximum frequency
//make a vector of max_element+1 and then store frequencies and then find the maximum element in the whole array and then pop it and then find second element of highest frequeny and so-on till k becomes zero
//teacher has used unordered map