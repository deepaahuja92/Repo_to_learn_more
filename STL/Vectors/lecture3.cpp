// pop_back()-->used to remove last element
// front()-->reference to first element
// back()-->reference to last element
// insert()-->used to insert an element
//v.clear()-->clears the whole vector
//v.empty()-->checks if the vector is empty
//v.resize()-->resizes the size of element
//v.resize(5)
//v.resize(8,100) remaining values will be 100
// erase()
// v.erase(v.begin())
// v.erase(v.begin(),v.end()-1)
//auto it=insert(v.begin(),100)
